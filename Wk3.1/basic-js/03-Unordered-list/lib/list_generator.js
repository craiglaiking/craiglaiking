listItem = (content) => {
const item = `<li class="list-group-item">${content}</li>`;
return item
  // TODO: return the proper <li> HTML tag with its content (as a string)
};

unorderedList = (items) => {
  // TODO: return the proper <ul> markup (as a string)
}
